services:
  - type: web
    name: bildbearbeitung
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "7860"
      - key: PYTHON_VERSION
        value: "3.13.11"
